import 'package:flutter/material.dart';

///
/// InitState
///
@immutable
class InitState {
  InitState();

  factory InitState.initial() {
    return InitState();
  }

  InitState copyWith() {
    return InitState();
  }
}
